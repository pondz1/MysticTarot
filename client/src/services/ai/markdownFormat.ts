/**
 * Re-export single source of truth from /shared/ai/markdownFormat.ts
 */
export {
  buildMasterDirectives,
  MARKDOWN_OUTPUT_RULES,
  buildStructureBlock,
  lifeAspectSections,
  buildFallbackMarkdown,
  type PredictionSection,
} from '@shared/ai/markdownFormat';
